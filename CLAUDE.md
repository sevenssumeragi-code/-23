# CLAUDE.md — 『ミッドナイトライン ―澪原市いのちの電話―』開発ルール

このファイルはリポジトリ直下に置くこと。Claude Code は毎回これを読んでから作業する。

---

## 1. プロジェクト概要

電話相談サスペンス × 心理ホラー × ミステリー × ストーリーシミュレーション。
プレイヤーは澪原市の夜間電話相談員。現場へは行けず、声・息遣い・物音・沈黙・雑音・話の矛盾だけで相談者を救う。

- 設計書（正典）：`docs/design.md`
- 実装：バニラJS。単一HTMLで動作すること（外部ライブラリ・ビルド依存を追加しない）
- 三重底の構造（ネタバレ注意・全設計の前提）
  1. 表：澪原市連続失踪事件の犯人は誰か
  2. 裏：5歳のムニの電話は「十年前」から掛かってきている
  3. 底：相談員である主人公自身が、十年前に消えた「七人目の子」

**迷ったら `docs/design.md` が正しい。実装が設計と食い違う場合は、勝手に直さず差分を報告する。**

---

## 2. ファイル構成

```
docs/design.md        完全設計書（正典・原則変更しない）
docs/ENGINE.md        エンジン仕様書
docs/FLAGS.md         全フラグ台帳（新規追加時は必ずここに追記）
src/index.html        HTML + CSS
src/engine.js         エンジン
src/scenario/pro.js   序章
src/scenario/ch1..ch5.js, fin.js
build.js              単一HTMLへバンドル
tools/validate.js     ラベル参照の到達性チェック
tools/sim.js          全ルート自動プレイ検証
dist/midnightline.html 生成物（手で編集しない）
```

---

## 3. 絶対に守ること（違反＝作業のやり直し）

1. **既存のフラグID・ラベル名・録音IDを変更しない。** 分岐が静かに壊れる。改名が必要なら理由を提示して確認を取る。
2. **新規フラグは必ず `docs/FLAGS.md` に追記。** 台帳にないフラグを使わない。
3. **`{go:}` `{then:}` `{els:}` の飛び先ラベルは、同じ章ファイル内に必ず実在させる。** 章をまたぐ移動は `{chap:'ch3'}` のみ。
4. **キャラの口調仕様（第5節）を一言一句守る。** とくにゲルの一人称と、ネオの二人称。
5. **エンディング判定 `judge()` の条件を勝手に緩めない。** 1周目でTRUEに到達できないのは仕様（喪失を体験させる設計）。
6. **`localStorage` / `sessionStorage` を使わない。** 保存は `window.storage`（失敗時はメモリ）にフォールバックする既存実装を踏襲。
7. **外部素材を増やさない。** 音はWebAudioの合成、スチルはCSSグラデーションで表現する既存方針を維持。
8. 加筆・改修のあとは必ず `node tools/validate.js && node tools/sim.js` を実行し、結果を報告する。

---

## 4. シナリオのノード記法（`SCENARIO.chX = [...]`）

| 記法 | 意味 |
|---|---|
| `{n:'label'}` | ジャンプ先ラベル |
| `{c:'LEN', say:'…'}` | セリフ。`nm:` で表示名上書き、`slow:1` で低速表示、`w:1` で囁き表示 |
| `{nar:'…'}` | 地の文 | 
| `{sys:'…'}` | システム表示（端末のログ・ニュース・逆探知結果など。HTML可） |
| `{me:'…'}` | 主人公の発言（通常は選択肢側で出す） |
| `{ch:[...], sec:12}` | 選択肢。`sec` は制限時間（秒） |
| `{go:'label'}` / `{if:cond, then:'a', els:'b'}` | 制御 |
| `{set:{FLAG:1}}` / `{inc:{COUNTER:1}}` | フラグ・カウンタ |
| `{par:{c:'LEN',trust:5,fear:-3,stress:2,doubt:0}}` | パラメータ増減 |
| `{meet:'LEN', known:[...]}` / `{know:['LEN','…']}` | 相談者の登録・判明情報 |
| `{life:['HYU','消失']}` | 生死（生存／危篤／死亡／消失） |
| `{memo:['見出し','本文']}` `{evd:[...]}` `{tl:['日付','内容']}` `{tlfix:[...]}` `{rel:['A','関係','B',点線?]}` `{vm:['見出し','内容']}` | 資料への追加。`tlfix` は年表の空白1行を書き換える専用 |
| `{rec:{id,ti,tx,hid,flag}}` | 録音登録。`hid` が0.5倍速／逆再生で出る隠し音声、`flag` はその発見で立つフラグ |
| `{still:'s06'}` | スチル解放（閉じると自動で進行） |
| `{noise:0..3}` `{fx:'shake'|'flash'}` `{ring:'normal'|'old'|'broken'}` `{amb:0..6}` | 演出 |
| `{call:{who,c,num}}` / `{hang:'理由', hangKind:'normal'|'cut'|'silent'}` | 通話開始・終了。`silent` は切電音が鳴らない＝消失演出 |
| `{clock:'03:33'}` `{day:7}` `{card:['CHAPTER 3','かがみめ編','DAY 7',3]}` `{chap:'ch4'}` | 時間・章 |

**選択肢オプション**

```js
{t:'表示文', tag:'傾聴', line:'実際に発話する文（省略時はtと同じ）',
 silent:1,                 // 沈黙選択肢（斜体表示）
 timeout:1,                // 制限時間切れ時に選ばれる
 req:{tr:['LEN',60]},      // 条件未達なら選べない（グレー表示）
 hide:{loop:2},            // この条件を満たすときだけ出現する隠し選択肢
 eff:{'LEN.trust':10}, set:{FLAG:1}, inc:{CNT:1}, go:'label'}
```

条件DSL：`{f:'FLAG'}` `{nf:'FLAG'}` `{tr:['LEN',60]}` `{dbt:['GER',40]}` `{alive:'HYU'}` `{loop:2}` `{cnt:['X',2]}` `{and:[…]}` `{or:[…]}`

タグの語彙（勝手に増やさない）：傾聴／受容／確認／指摘／説得／核心／教える／伴走／連携／照合／約束／配慮／記録／発信／受電／見送り／切電／沈黙／保留

---

## 5. キャラクター口調仕様（厳守）

| 名前 | 一人称 | 話し方 | 呼称 |
|---|---|---|---|
| レニィ | 僕 | ほわほわ天然。「〜だよ」「〜だね」。まれに「〜だよぉ」と語尾を伸ばす抜けた喋り方 | ジンパチ・ヒュウを呼び捨て |
| ヒュウ | 私 | ナルシストで気取り屋。常に敬語 | レニィは呼び捨て、「ジンパチくん」 |
| ジンパチ | 俺 | 俺様系・熱血。荒い口調 | 呼び捨て |
| ムニ | ムニ（自称） | 5歳。ひらがな主体。甘えん坊で寂しがり | 「おにいちゃん」「でんわのひと」 |
| ゲル | **私** | 女性らしくない口調。「〜だな」「〜だ」 | **二人称に「貴様」を絶対に使わない** |
| ネオ | 私 | 高飛車・高圧的。二人称は**貴様**。「〜だ」「〜だな」等の高貴な語尾 | — |

- 髪・目：レニィ＝青髪青目／ヒュウ＝緑髪・前髪で隠した赤い右目／ジンパチ＝茶髪ツンツン／ムニ＝青髪／ゲル＝紫のセミロングボブ／ネオ＝金髪ロング（正体は異空間〈渡岸〉の魔法剣士）
- ネオの正体は第四章まで伏せる。それ以前の地の文で「剣士」と書かない。

---

## 6. 本文執筆のルール

1. 各通話に「声・息遣い・物音・沈黙・雑音・矛盾」の6要素を配分する。ひとつも入らない通話は書き直す。
2. 各章に「後から意味が反転する台詞」を最低3つ。コード上に `// 【反転】回収=ch5` とコメントする。
3. 章末は必ず ①向こう側から切れる ②留守電1件を残す ③次章の疑問を `{sys:}` で提示、の3点で締める。
4. 恐怖は音で表現する。ノイズ段階（0〜3）と `hangKind:'silent'`（切電音が鳴らない）を優先し、直接的な描写に頼らない。
5. 説明しない。プレイヤーが録音を聴き直して自分で気づける形に情報を置く。**答えを地の文で言わない。**
6. 選択肢は「言い方の違い」ではなく「相手の運命の違い」を作る。同じ結果に収束する選択肢は作らない。
7. 未成年（レニィ・ヒュウ・ジンパチ・ムニ）に恋愛的・性的な描写を一切入れない。

---

## 7. フラグとエンディング（変更禁止の骨格）

- 命名：`[対象]_[種別]_[連番]`（例 `HYU_STAY_01`）。周回引き継ぎ対象は台帳に★を付ける。
- 生死判定に関わる主要フラグ：`JIN_RUSH_01` `GER_KNIFE_01` `HYU_STAY_01` `NEO_HYU_01` `LEN_REMEMBER`
- TRUE条件：`KAI_EXPOSE_01` + `GER_KNIFE_01` + `JIN_RUSH_01` + 5人生存 + `MUN_TEACH_01/02/03` + `MUN_CUT_XX < 2` + `EVD_TAPE_01` + `EVD_WAVE_01` + `NEO_HYU_01` + `STORY_TRUE_KEY`
- `MUN_TEACH_02`（物置の鍵）は **2周目限定**。これが1周目でTRUEに届かない理由なので、条件を緩めない。
- `EVD_WAVE_01` と `KAI_EXPOSE_01` は録音の隠し音声を聴かないと立たない。**録音再生を迂回できる導線を作らない。**
- SECRET は TRUE 到達後、2周目以降の序章で隠し番号へ発信（`SEC_CALL_01`）した場合のみ。

---

## 8. 作業の進め方

1. 着手前に方針を箇条書きで提示し、確認を取る。長い改修を無断で始めない。
2. 加筆は**章単位**で行う。複数章を一度に書き換えない。
3. 変更後は必ず実行し、結果を報告する：
   ```
   node build.js && node tools/validate.js && node tools/sim.js
   ```
   - validate：全ラベル参照の実在確認（missing 0 であること）
   - sim：自動プレイ。期待値は
     「1周目・録音未使用→NORMAL／1周目・録音使用→GOOD／2周目→TRUE・SECRET／最悪手→BAD」
     この期待値が崩れたら、直す前にまず報告する。
4. コミットメッセージは日本語で、章名と変更意図を書く（例：`ch3: 追跡シーンの伴走選択を加筆、HYU_STAY_01の分岐差を強化`）。

---

## 9. やらないこと

- `dist/` を直接編集する
- 設計書の事件年表・真相・キャラ設定を独断で変更する
- 「わかりやすさのため」に伏線の答えを地の文で先出しする
- 選択肢を増やしすぎて1画面に収まらなくする（モバイル基準：同時表示は最大4つ）
- 演出の追加でテキストの可読性を落とす（ノイズ3は短時間のみ）
