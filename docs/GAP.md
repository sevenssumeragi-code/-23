# 設計書と実装の差分

`docs/design.md`（完全設計書）と `game/midnightline.html`（実装）を突き合わせた一覧。
エンジン側の仕様は `docs/ENGINE.md` を参照。

分類:

- **未実装** — 設計にあるが実装に無い
- **簡略化** — 実装はあるが設計より縮んでいる
- **追加** — 設計に無いが実装にある
- **不一致** — 双方にあるが内容が食い違っている
- **バグ** — 実装の誤り（本リポジトリで修正済みのものを含む）

---

## 1. 修正済みの実装バグ

分割の過程と検証スクリプトの導入で見つかった、元の単一HTML版からあったバグ。
コミット `f154c3b`（5件）と `58b1ec5`（2件）で修正済み。

| # | 症状 | 原因 | 影響 |
|---|---|---|---|
| 1 | GOOD / TRUE / SECRET に到達できない | `judge()` が `const 现 = [...]` と宣言して `allAlive` を参照。`goodBase && allAlive` は `goodBase` が偽なら短絡するため BAD/NORMAL では露見せず、`goodBase` が立った瞬間だけ `ReferenceError` を投げて `endGame` ごと停止 | **致命**。GOOD 以上の分岐が全て死んでいた |
| 2 | 第五章でゲームが止まる | `{meet:'KAI'}` の灰堂は `TRACKED` 外で `S.chars['KAI']` が `undefined`。そこへ `.known` を代入して `TypeError` | **致命**。本筋の位置にあり、そもそもどのENDにも到達できなかった |
| 3 | 2周目で証拠照合ができない | `playRec()` が `r.opened` を見て初回だけフラグを立てる。`opened` は `G.recs` 経由で周回に引き継がれるがフラグは周回ごとにリセットされるため、2周目に聴き直しても `EVD_KAI_STEP` 等が立たない | **致命**。TRUE は2周目以降でしか到達できない設計なので TRUE / SECRET が構造的に到達不能 |
| 4 | 「次の周回へ」で落ちる | `startGame()` が `$('#title').remove()` を無条件に呼ぶが 2 回目以降は `#title` が既に無く `null` | 周回に入れない。エンディング画面も残り続ける |
| 5 | 「続きから」でタイトルが消えない | `resume()` を呼ぶだけで `#title` を除去していない | ロード後の画面がタイトルに覆われる |
| 6 | リロードでセーブが消える | `window.storage`（非標準API）のみを見ており、素のブラウザでは常に失敗してメモリ退避に落ちる | 実質セーブ不可。周回引き継ぎも消える |
| 7 | `se:` が常に既定音 | `{}[nd.se]` は常に `undefined` | 現状シナリオ未使用のため実害なし |

> 1・2・3 が同時に存在したため、**元の実装では BAD と NORMAL 以外のどのエンディングにも到達できなかった**
> （厳密には 2 のクラッシュにより、第五章を通過した時点でどのENDにも到達できない）。

---

## 2. 未実装 — 電話システム（design §2-1）

| 設計 | 状態 | 備考 |
|---|---|---|
| 同時着信（最大3回線）と優先順位の分岐 | **未実装** | 終章の「三本の回線」は演出としてのみ存在し、実際は `line1 → line2 → line3` の直列。`THREE_LINES` フラグは立つが分岐に影響しない |
| 通話時間のリアルタイム制／AP制 | **簡略化** | 選択肢ごとに固定 2 分（`cost`）消費するだけ。長話で他の着信を逃す仕組みは無い。`cost` はシナリオ側で一度も使われていない |
| 保留（別回線へ切替、保留中の精神状態変動） | **未実装** | 終章に「保留を使う」選択肢はあるがフラグを立てるのみ |
| 折り返し電話（時間帯・信頼度・生死依存） | **未実装** | 地の文としては登場するがプレイヤー操作は無い |
| 発信（入手した番号にのみ発信可） | **簡略化** | 序章の隠し番号 `090-XXXX-1994` のみ。番号一覧からの能動的発信は無い |
| 留守番電話 | **実装済み** | パネルから再生可能。全10件 |
| 録音再生（波形表示・逆再生・0.5倍速） | **簡略化** | 波形はヘッダの装飾のみで録音とは連動しない。`再生` / `0.5倍速` / `逆再生` の3ボタンがあるが、**0.5倍速と逆再生は同一処理**（どちらも `mode !== 'play'` で裏内容を開く） |
| 通話履歴 | **実装済み** | ただし「通話時間0秒の着信」「存在しない局番」は本文中の演出で、履歴データとしては再現されていない |

## 3. 未実装 — 情報管理システム（design §2-2）

| 設計 | 状態 | 備考 |
|---|---|---|
| 相談メモの自動ハイライト | **未実装** | メモはシナリオが `memo` ノードで明示的に積むだけ |
| プレイヤーが付ける「信じる／疑う」マーカー | **未実装** | フラグ化する仕組みごと無い。設計では「これがフラグになる」とされている中核機能 |
| 人物相関図 | **実装済み** | ただし線は `rel` ノードで手動追加。周回時の「隠し線（点線）」は `rel[3]` で表現できるが、**シナリオ中の点線8本はすべて周回を問わず引かれる**（周回限定の隠し線は存在しない） |
| 事件年表の空白1行 | **実装済み** | `newState` で index 1 に確保され、`tlfix` で 2 箇所から埋まる（第五章＝ムニの死／終章＝死者ゼロ）。設計意図どおり |
| 証拠の「照合」演出（矛盾する証拠を重ねる） | **簡略化** | 選択肢の `req` で「三点が繋がる」判定をするのみ。カードを重ねる操作は無い |
| 相談者プロフィールの声紋アイコン | **未実装** | 名前・属性・判明情報のテキストのみ |
| 嘘が暴かれたときの赤線訂正 | **未実装** | |

## 4. パラメータ（design §2-3）

| 設計 | 状態 |
|---|---|
| 信頼度 / 疑念 / 恐怖 / ストレス | **実装済み**（0〜100・右ペインにバー表示）|
| 精神状態（安定/動揺/混乱/危機） | **実装済み**（恐怖とストレスから自動導出）|
| 生死（生存/危篤/死亡/消失） | **簡略化**。`危篤` は定義だけで**シナリオ中で一度も使われない**。実際に使われるのは `消失`（6箇所）と `死亡`（2箇所）|
| 疑念の効果「高いと重要情報を隠す／偽情報を流す」 | **未実装**。疑念を上下させる `eff` は12箇所あり値は動くが、`dbt` 条件が**シナリオ中で一度も使われない**ため、上げても下げても何も起きない死にパラメータになっている |
| ストレスの効果「蓄積で着信頻度低下」 | **未実装**。着信は完全に固定進行 |
| 「精神状態で選択肢の残り時間が変化」 | **未実装**。`sec` は固定値 |

## 5. 時間システム（design §2-4）

| 設計 | 状態 |
|---|---|
| Day1〜14 | **実装済み**。Day1〜14 が飛びなく設定される（各章2日ずつ）。ただし設計の章割当（例: 二章 Day4〜6）とは1日ずつずれる |
| 3:33 の特殊現象 | **実装済み**。ムニ回線が開く時刻として一貫している |
| 幕間の「昼のニュース／SNS画面」 | **簡略化**。第二章に `sys` ノードでニュース文が2回出るのみ。**選択結果の反映も、周回差分の可視化も無い** |

## 6. 章構成（design §4）

序章＋全6章＋終章という骨格、および各章の主要事件は**設計どおりに実装されている**。

| 章 | 設計の Day | 実装 | 主要事件 |
|---|---|---|---|
| 序章 | Day1 | Day1 | レニィ初通話、雨音、名札 ✓ |
| 一章 うたたね | Day2〜4 | Day2〜4 | 3:33のムニ着信、ずっと夏、夏祭り ✓ |
| 二章 ともしび | Day4〜6 | Day5〜6 | ジンパチ、カナタ失踪、ゲルの探り、波形照合 ✓ |
| 三章 かがみめ | Day6〜8 | Day7〜8 | ヒュウの右目、追跡、消失 ✓ |
| 四章 よふかし | Day8〜10 | Day9〜10 | ネオ混線、逆探知＝十年前の灯守園 ✓ |
| 五章 ほむら | Day10〜12 | Day11〜12 | ゲルの正体、室井テープ、レニィ失踪 ✓ |
| 終章 あさやけ | Day13〜14 | Day13〜14 | 三回線、ムニ脱出、裂け目閉鎖 ✓ |

**未実装の共通ルール**

- 「各章に後から意味が反転する台詞を最低3つ」— 台本上の `【反転】` タグ管理は無い（本文には仕込まれている）
- 「章末は必ず①強い引き ②留守電1件」— 留守電は序章・一〜五章の章末に置かれており**ほぼ守られている**（終章のみ無し、性質上妥当）

## 7. キャラクター（design §5）

口調仕様（§5冒頭）は本文で厳守されている。ゲルの一人称「私」、ネオの二人称「貴様」も一貫。

| キャラ | 死亡ルート | 救済ルート | TRUEルート |
|---|---|---|---|
| レニィ | ✓ 五章で `life:['LEN','消失']` | ✓ | ✓ |
| ヒュウ | ✓ 三章 `消失` ／ 終章 `死亡` | ✓ | ✓ |
| ジンパチ | ✓ 終章 `消失`（2経路）| ✓ | ✓ |
| ムニ | ✓ 終章 `死亡` | ✓ 刷り込み3点 | ✓ |
| ゲル | ✓ 終章 `消失` | ✓ | ✓ |
| ネオ | ✓ 終章 `消失` | ✓ | ✓ |

- **不一致**: ヒュウの死亡ルートについて設計は「三章で『電話を切って走れ』と指示 → 渡岸の深部で衰弱死」。実装では三章で `消失` になり、終章で `NEO_HYU_01` 未成立かつ生存していなければ `死亡` に変わる二段構え。設計より緩い
- **不一致**: 設計の死亡ルート一覧では**灰堂の結末に BAD/GOOD/TRUE の3分岐**が指定されているが、実装では BAD 時の「街ごと消失」はエンディング文面のみで、灰堂の逮捕・摘発は分岐せず `KAI_EXPOSE_01` の有無だけで表現される
- **未実装**: 室井の「終章でネオ回線越しに一言だけ交信できる」（涙演出の要、design §10涙4「切らないで、くれたんだな」）。実装では**室井はテープの中の声としてのみ登場**し、終章での交信は無い

## 8. エンディング（design §6）

| END | 設計の条件 | 実装の条件 | 差分 |
|---|---|---|---|
| BAD | 主要生存者2名以下／裂け目閉鎖失敗 | `LEN/JIN/GER` の生存が2名未満 | **不一致**。裂け目閉鎖（`RIFT_CLOSED`）は判定に入っていない |
| NORMAL | 現在の事件は阻止／ムニ死亡／灰堂未解明 | 上記以外で `LEN/JIN/GER` が2名以上生存 | ほぼ一致 |
| GOOD | 灰堂逮捕＋剛三冤罪証明＋現在組全員生存 | `KAI_EXPOSE_01 && GER_KNIFE_01 && JIN_RUSH_01 && 5名全員生存` | 一致（冤罪証明は `GER_KNIFE_01` が兼ねる）|
| TRUE | GOOD条件＋ムニ救出3条件＋録音証拠全照合＋ネオ&ヒュウ共闘 | GOOD条件＋`MUN_TEACH_01/02/03`＋`MUN_CUT_XX < 2`＋`EVD_TAPE_01`＋`EVD_WAVE_01`＋`NEO_HYU_01`＋`STORY_TRUE_KEY` | **「録音証拠全照合」は2件のみ**。設計の `EVD_REC_S1〜S7` 全回収は要求されない |
| SECRET | TRUE後2周目でDay1に隠し番号へ発信＋全隠し録音回収 | シナリオが `ep_secret` で `{end:'SECRET'}` を直接指定。実質 **TRUE経路＋`SEC_CALL_01` のみ** | **不一致**。`judge()` 内には設計どおりの条件（`G.recs>=5` かつ `G.endings` に TRUE）が書かれているが、**この分岐には到達しない**（§9-2 参照）。隠し録音の全回収も、TRUE クリア済みであることも実際には要求されない |

### judge() の SECRET 分岐がデッドコードである件

`SEC_CALL_01` が立ち TRUE 条件も満たす状況では、シナリオ側が `ep_secret` へ分岐して
`{end:'SECRET'}` を直接実行する。よって `{end:'check'}` に `SEC_CALL_01` 付きで到達する経路が無く、
`judge()` の SECRET 判定は評価されても真にならない。
設計どおりの厳しい条件を効かせたいなら、`ep_secret` 側にも同じ `req` を課す必要がある。

## 9. フラグ（design §7）

設計の主要フラグ表と実装の対応。

| 設計のフラグID | 実装 | 備考 |
|---|---|---|
| `SYS_TUTO_01` | **無し** | UI解放の概念自体が無い（最初から全パネル利用可）|
| `LEN_TRUST_01〜05` | **無し** | 段階フラグではなく `tr:['LEN', n]` の直接判定 |
| `MUN_LINE_01` | ✓ | |
| `MUN_CUT_XX` | ✓ | カウンタ。切電・見送りで +1、`< 2` が TRUE 条件 |
| `MUN_TEACH_01/02/03` | ✓ | |
| `JIN_LIE_01` | ✓ | |
| `JIN_RUSH_01` | ✓ | |
| `HYU_EYE_01` | ✓ | |
| `HYU_STAY_01` | ✓ | |
| `GER_ID_01` | ✓ | |
| `GER_KNIFE_01` | ✓ | |
| `NEO_ALLY_01` | ✓ | |
| `NEO_HYU_01` | ✓ | |
| `EVD_TAPE_01` ★ | ✓ | **★（周回引き継ぎ）ではない**。フラグは周回ごとにリセットされる |
| `EVD_WAVE_01` ★ | ✓ | 同上 |
| `EVD_REC_S1〜S7` ★ | **部分的** | 録音の裏内容フラグは7件あるが名前が異なり（`EVD_SONG_01` `EVD_KAI_STEP` `EVD_HZ_01` `EVD_KAI_STEP2` `NEO_VOICE_01` `SEC_HINT_01` `SEC_HINT_02`）、SECRET 条件としては使われない |
| `KAI_EXPOSE_01` | ✓ | |
| `SAVE_LEN/JIN/GER/HYU/NEO` | **無し** | 生死は `life` で直接管理し、判定は `alive()` で行う |
| `STORY_TRUE_KEY` | ✓ | |
| `LOOP_02` ★ | ✓ | 周回時に周回数が入る |
| `SEC_CALL_01` | ✓ | |

**★（周回引き継ぎ）の実装状況**: 設計では `EVD_TAPE_01` `EVD_WAVE_01` `EVD_REC_S1〜S7` `LOOP_02` が
引き継ぎ対象とされているが、実装が引き継ぐのは **資料（メモ・証拠・相関図・年表・スチル）と
録音の暴露状態（`G.recs`）だけ**で、**フラグは `LOOP_02` を除きすべてリセットされる**。
これは design §12「パラメータと信頼度はリセット（会話し直す誠実さを要求）」の拡大解釈にあたる。
結果として2周目でも第五章でテープを取り直し、録音を聴き直す必要がある。

## 10. 演出（design §8〜10）

| 設計 | 状態 |
|---|---|
| スチル18枚 | **実装済み**。18枚すべて定義され、CSSグラデーションによる抽象表現で代替。取得条件も設計どおり |
| 章別BGM 8曲 | **簡略化**。WebAudio の低周波ドローン（章ごとに基音を変える）のみ。旋律は無いので「半音ずれたオルゴール」「短調アレンジ」「主題歌」といった伏線は音では表現されない |
| 着信音3種 | **実装済み**（通常 / 古い黒電話 / 割れたベル）|
| 切電音3種 | **実装済み**（プツッ / ツーツー / **無音**＝消失フラグ音）|
| 保留音・囁きの混入 | **未実装** |
| 低周波（14Hz付近を実際に混ぜる） | **未実装**。本文とノイズ演出でのみ表現 |
| ノイズ段階0〜3 | **実装済み**。画面効果とヘッダ表記で表現。設計の「ゲージ非表示のまま音だけで伝える」に対し、**実装ではヘッダに「境界接近 — 回線不安定」と文字で出る** |
| 「沈黙する」「相槌だけ打つ」の常設選択肢 | **簡略化**。`silent:1` の沈黙選択肢は17箇所あるが、常設ではない |
| 逆再生で意味が通る囁き | **簡略化**。テキストとして裏内容が出るだけで、音の逆再生は無い |
| 感情演出カタログ（鳥肌10・涙6） | **部分実装**。鳥肌1（最初から囁きが入っている録音）、2（祭りの日付一致）、3（現存しない番地）、6（Day1より前の記録）、10（2周目のレニィ第一声）は本文に存在。鳥肌4・5・7・8・9 と涙4（室井の交信）は未実装 |
| TRUE最終「受ける一択しか出ない」（涙6） | **実装済み**。`ep_true` の選択肢は「受話器を取る」1つのみ |

## 11. 周回構造（design §12）

| 設計 | 状態 |
|---|---|
| 1周目は GOOD までしか到達不可 | **実装済み**。`MUN_TEACH_02` の選択肢が `hide:{and:[{loop:2},{f:'EVD_TAPE_01'}]}` で塞がれている |
| 2周目解放: 隠し選択肢 | **実装済み**。3箇所（序章の隠し発信、一章の録音準備、終章の物置の鍵）|
| 2周目解放: 逆再生・0.5倍速の全通話開放 | **未実装**。1周目から全録音で使える |
| 2周目解放: レニィ既視感イベント | **未実装**。設計の「あれ? 前にも話したこと、あるよね?」は本文に無い。2周目差分は序章の名札裏（`s18` スチル＋「次の子へ」）のみ |
| SECRET は3周目 | **不一致**。実装は2周目で到達可能 |

---

## 12. 設計に無い追加要素

| 追加 | 内容 |
|---|---|
| `tlfix` ノード | 事件年表の空白行を上書きする専用ノード。設計の「空白1行」を機能として具体化したもの |
| `hide` による周回限定選択肢 | 設計の「点線の隠し選択肢」を、条件不成立時は**存在自体を見せない**形で実装 |
| `req` によるロック表示 | 条件を満たさない選択肢を「— まだ言葉が見つからない」と表示する。設計に記述の無い演出 |
| フラグ確認パネル | デバッグ兼考察用に、立っているフラグを全件表示するパネルがある |
| `toastx` ノード | シナリオが使うのにエンジンに実装が無かった。本リポジトリでムニへの刷り込み到達数（n/3）を表示する形で実装 |
| 終章の `FIN_RUN` / `FIN_ALLY` | ムニ脱出パートの演出用フラグ。判定には影響しない |
| ノイズレベルのヘッダ文字表示 | 設計は「音だけで伝える」としているが、実装は文字でも出す |

---

## 13. 未着手として大きいもの（優先度つき）

実装するなら効果が大きい順。

1. **室井の終章交信**（design §10涙4）— 「切らないで、くれたんだな」。設計上、涙演出の要とされている。`MRI` キャラと `MRI_TAPE_HEARD` フラグは既にあるので接続だけで足りる
2. **メモの「信じる／疑う」マーカー**（design §2-2）— 設計では明確に「これがフラグになる」中核システム。現状は疑念パラメータもほぼ死んでいるため、両方まとめて活かせる
3. **幕間ニュースへの選択結果の反映**（design §2-4, §4）— 周回差分の可視化という設計意図が現状ほぼ機能していない
4. **SECRET 条件の設計への引き戻し**（§8）— `ep_secret` に `req` を課し、隠し録音の全回収と TRUE クリア済みを要求する
5. **同時着信と保留**（design §2-1）— 「どの声を優先するか」という設計の核。ただしエンジンの実行モデル（単一 pc）に手を入れる必要があり、影響範囲は大きい
6. **周回時のレニィ既視感イベント**（design §12）— 本文の追加のみで実装でき、費用対効果が高い
